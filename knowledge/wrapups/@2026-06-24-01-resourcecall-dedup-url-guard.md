# Wrapup: ResourceCall dedup-path URL guard + knowledge base seed

**Date:** 2026-06-24
**Branch:** legacy
**Baseline:** start of day (no prior wrapup)
**HEAD at wrapup:** 79ddba7203fe23be13b62647532f23c7f8fcb084
**Note:** —

---

## Summary
Fixed a latent crash in `ResourceCall` where an errored request on the
dedup/shadow path threw a `TypeError` that masked the real HTTP error. Also
seeded a `knowledge/` base and an `AGENTS.md` describing Kawax's reducer/action
routing and component re-render gotchas. No commits landed today — all work is
uncommitted; this is the first wrapup on this branch.

## What We Did
- **`ResourceCall` error path** (`src/internal/ResourceCall.js:119` + both
  `dist/cjs` and `dist/es` builds) — guarded `this.preflightRequestUrl.toString()`
  with a null check. `preflightRequestUrl` is left unset when an identical
  in-flight request short-circuits (dedup/shadow path), so a deduped request that
  then errors (e.g. a 404) previously threw a `TypeError` in the error-payload
  builder and masked the real HTTP error. Now emits `url: null` instead.
- **Knowledge base seed** — added `knowledge/INDEX.md`,
  `knowledge/js-kawax-reducers.md`, `knowledge/js-kawax-components.md` documenting
  the `DOMAIN.SLICE.VERB` type→reducer routing and two component re-render gotchas
  (stable `actions` ref defeating prevProps edge-detection; latch a local flag).
- **`AGENTS.md`** — top-level agent guidance pointing at the knowledge base.

## Current Status
- The `ResourceCall` fix is complete in source and in both committed build
  outputs (`dist/cjs`, `dist/es`), so it is consistent without a rebuild.
- Knowledge base and `AGENTS.md` are authored and untracked; ready to commit.
- **Could not run lint or the test suite** — the local dev toolchain is not
  installed (see Open Issues). The fix is unverified by automated tests.

## Open Issues / Risks
- [ ] **Test suite cannot run** — `jest.config.js` `setupFilesAfterEnv` points at
  `./node_modules/jest-enzyme/lib/index.js`, but `jest-enzyme` (and `enzyme`) are
  neither installed nor listed in `package.json` devDependencies. Jest aborts at
  config validation. Pre-existing, unrelated to this change.
- [ ] **Lint cannot run** — local `eslint` is not installed under `node_modules`,
  so `eslint-config-airbnb-base` fails to resolve `eslint/package.json` (it falls
  back to a version-mismatched global ESLint). Pre-existing, unrelated to this
  change. A clean `yarn install` (adding the missing test deps) is likely needed.
- The `ResourceCall` fix is a defensive guard; behavior on the non-dedup path is
  unchanged (`preflightRequestUrl` is truthy there), so risk is low.

## Pending / Cleanup
- [ ] Add `enzyme` + `jest-enzyme` (and any React 16 adapter) to devDependencies,
  or update `jest.config.js` to match the actual test stack, so the suite runs.
- [ ] Add a local `eslint` devDependency compatible with `eslint-config-airbnb`
  so `yarn lint` works without the global fallback.
- [ ] Add a regression test for the deduped-request-error path once the suite is
  runnable (red-green per guideline P1).

## Next Steps
1. Run a clean `yarn install` and reconcile the missing test/lint deps so the
   suite and linter run, then verify the `ResourceCall` fix.
2. Add the deduped-error-path regression test.
3. Commit the knowledge base + `AGENTS.md` alongside the fix (this wrapup does so).

## Notes & Decisions
- Kept the build outputs (`dist/`) edited in lockstep with `src/` since `dist/`
  is committed and shipped (`package.json` `files`), avoiding a separate rebuild.
- Wrapup proceeded despite a red toolchain because both failures are pre-existing
  environment/config gaps, not regressions from this change.

## Lint & Test Status
- ESLint: ⚠️ cannot run — local `eslint` missing (pre-existing env issue).
- Jest: ⚠️ cannot run — `jest-enzyme` setup file missing (pre-existing env issue).

---

## Changed Files (since baseline)
```
 dist/cjs/internal/ResourceCall.js | 6 +++++-
 dist/es/internal/ResourceCall.js  | 6 +++++-
 src/internal/ResourceCall.js      | 6 +++++-
 3 files changed, 15 insertions(+), 3 deletions(-)
 (untracked) AGENTS.md
 (untracked) knowledge/INDEX.md
 (untracked) knowledge/js-kawax-reducers.md
 (untracked) knowledge/js-kawax-components.md
```

## Commits (since baseline)
```
(none — no commits today; all work was uncommitted)
```

---
*Generated by `/forge:wrapup`. Load the latest wrapup with `/forge:resume`.*
